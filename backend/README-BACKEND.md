# Backend — Candidate Referral Management System


## Setup


1. Create project folder and backend subfolder.
2. Copy files into `backend/` as listed.
3. Create a `.env` file (copy from `.env.example`) and set `MONGO_URI`.
4. Create uploads folder (the middleware will create it automatically, but you can create manually):


```bash
mkdir backend/uploads